<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- For camera temp files written to getCacheDir() -->
    <cache-path
        name="camera_images"
        path="." />
    <!-- For gallery images chosen from external storage -->
    <external-path
        name="external_images"
        path="." />
</paths>
